/* SolarMeerkat.controller("PlaningReportsCtrl", function($scope, $http){
    $scope.devices = [
        {
            done: false
        }
    ];
    $scope.addDevice = function() {
        devices.push($scope.deviceParam, $scope.deviceParam2, $scope.deviceName);
    };
    $scope.deleteDevice = function() {
        devices.splice(device, 1);
    };
};
*/
